1. rescue from mailgun errors. Investigate other gaps in error handling while doing so.
2. pull in default mailgun url configs from ENV in config/environments/production.rb and config/environments/development.rb
3. convert auth pages (login, sign up, etc) into react for consistency ()